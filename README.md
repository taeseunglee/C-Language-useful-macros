# C-Language-useful-macros

## 1.log_debug
### The Least standard C version
C99, because of Variadic Macros.

### Usage Example
``` bash
gcc -D DEBUG c_test.c
g++ -D DEBUG cpp_test.cpp
gcc -D LOG c_test.c
gcc -D LOG_DATE c_test.c
```
